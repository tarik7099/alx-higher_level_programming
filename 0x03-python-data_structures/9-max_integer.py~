#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    
    for multiple in my_list:
        if multiple % 2 == 0:
            new_list = []
            new_list.append(multiple)
            print(new_list)
        else:
            return False