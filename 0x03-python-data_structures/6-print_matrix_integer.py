#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    if matrix and len(matrix) > 1:
        for i in range(0, 3):
            print("{} {} {}".format(matrix[i][0], matrix[i][1], matrix[i][2]))y
